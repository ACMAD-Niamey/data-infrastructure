DEBUG=False
DJANGO_SECRET_KEY=changeme
DJANGO_ALLOWED_HOSTS=*

POSTGRES_DB=geodatamanager
POSTGRES_USER=geodatamanager
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=db
POSTGRES_PORT=5432
STATIC_VOLUME=./staticfiles
MEDIA_VOLUME=./media
COMPOSE_PROJECT_NAME=geomgr
PGDATA_VOLUME=./geomgr_pgdata
UI_DIR=./e-safari-ui

TITILER_URL=https://climatehub.acmad.org/titiler
REPLACE_TITILER_URL=True


# Celery / Redis (configured below with REDIS_PASSWORD + REDIS_URL)
# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme
MINIO_ENDPOINT=minio:9000
MINIO_ENDPOINT_PUBLIC=http://minio.acmad.org:9000
MINIO_USE_SSL=false
MINIO_PUBLIC_ENDPOINT=http://minio.local
MINIO_PUBLIC_WEB_ENDPOINT=http://console.minio.local
MINIO_BUCKET=geodata
STAC_API_URL=http://stac_api:8080
MINIO_VOLUME=./data

## pg stack
PG_STAC_POSTGRES_DB=pgstac
PG_STAC_POSTGRES_USER=pgstac
PG_STAC_POSTGRES_PASSWORD=changeme
PG_STAC_POSTGRES_HOST=pgstac
PG_STAC_POSTGRES_PORT=5432
PORTAL_RASTERS_VOLUME=./data
WIS2_DOWNLOAD_DIR=/app/data/wis2_downloads
WIS2_DOWNLOAD_MOUNT_VOLUME=./data/wis2_downloads
CELERY_BEAT_CLEANUP_WIS2_DOWNLOADS_SCHEDULE_HOUR=21
CELERY_BEAT_CLEANUP_WIS2_DOWNLOADS_SCHEDULE_MINUTE=20
HTTPS_ENDPOINT_URL=https://climatehub.acmad.org


#Redis
REDIS_PASSWORD=changeme
REDIS_URL=redis://:changeme@redis:6379/0
TIPG_DEBUG=True

# nginx vhost config (mounted at /etc/nginx/nginx.conf):
#   default.conf          - local/dev, HTTP only, server_name-agnostic
#   default_pre_ssl.conf  - SSL bootstrap: HTTP only, serves ACME challenges
#   default_ssl.conf      - production: HTTPS + HTTP->HTTPS redirect + ACME
# First-time cert issuance:
#   1) set NGINX_CONFIG=default_pre_ssl.conf ; docker compose up -d --force-recreate nginx
#   2) make certificate
#   3) set NGINX_CONFIG=default_ssl.conf ; docker compose up -d --force-recreate nginx
NGINX_CONFIG=default.conf

# Domains for the Let's Encrypt SAN certificate (must all resolve to this host
# and be reachable on port 80). Consumed by the certbot / certbot-renew services.
DOMAIN_MINIO_CONSOLE=console-minio.acmad.org
DOMAIN_MINIO=minio.acmad.org
DOMAIN=e-safari.acmad.org
DOMAIN_MULTI_HAZARD=multi-hazard.acmad.org
EMAIL=admin@acmad.org

TIPG_CORS_ORIGINS=*
# Google reCAPTCHA v2
# RECAPTURE_PUBLIC_KEY=changeme
# RECAPTCHA_PRIVATE_KEY=changeme
